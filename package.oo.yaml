version: 0.0.2
icon: ./icon.png
scripts:
  bootstrap: |
    set -e
    sudo apt-get update
    sudo apt-get install -y unrar
    poetry install --no-root
dependencies:
  oomol-file: 0.0.14
name: manga-tools
displayName: Manga tools
description: convert manga file formats CBZ, CBR, EPUB, PDF to each other and
  archive and unarchive.
repository: https://github.com/oomol-flows/manga-tools
releaseNotes: add groups & descriptions.
