version: 0.0.1
icon: ./icon.png
scripts:
  bootstrap: |
    set -e
    sudo apt-get update
    sudo apt-get install -y unrar
    poetry install --no-root
dependencies:
  oomol-file: 0.0.12
  oomol-preview: 0.0.5
