version: 0.0.4
icon: ./icon.png
scripts:
  bootstrap: |
    set -e
    sudo apt-get update
    sudo apt-get install -y unrar
    poetry install --no-root
dependencies:
  oomol-file: 0.0.14
name: manga-tools
displayName: "%manga-tools%"
description: "%convert-manga-file-formats-cbz-cbr-epub-pdf-to-each-other-and-ar%"
repository: https://github.com/oomol-flows/manga-tools
releaseNotes: i18n
